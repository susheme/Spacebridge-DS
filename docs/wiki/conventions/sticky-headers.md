# Догма: хэдэр контейнера — sticky

Указание юзера, 14.09.2026 (ревью demo/app-host).

Хэдэр скроллящегося контейнера — страницы, карточки, панели — прилипает к
верху своей зоны прокрутки, а не уезжает вместе с контентом. Относится к
Header L/M/S на страницах и к `sb-card-header` карточек с собственным скроллом.

Как применять:

- **Предпочтительный приём для панелей — хэдэр ВНЕ скролл-зоны** (уточнение юзера
  14.09.2026): хэдэр — сиблинг над скролл-областью (паттерн 2 из доков Nav Bar).
  Скроллбар упирается в низ хэдэра и не заезжает за него; full-bleed и sticky
  при этом не нужны вовсе. `position:sticky` — для хэдэров, живущих в общем
  потоке контента (Section Header в длинном списке).

- `position: sticky; top: 0` + непрозрачный фон поверхности + z-index выше контента.
- **Паддинг скролл-зоны сверху = 0** (дополнение юзера 14.09.2026): иначе между
  верхом зоны и прилипшим хэдэром остаётся щель, в которой виден скроллящийся контент.
- **Full-bleed**: sticky-хэдэр расширяется на всю плоскость, где лежит, — отрицательные
  боковые margin на величину паддингов плоскости + те же паддинги внутрь. Аналог
  поведения Nav Bar Floating (is-stuck разворачивается до full-width).
- **Верхние радиусы сохраняются** (дополнение юзера 14.09.2026): прилипший хэдэр
  держит радиус верхних углов своей панели, скролл-зона клипает контент по тем же
  радиусам. В 0 радиус уходит только у Nav Bar — он упирается в углы окна браузера,
  панельным хэдэрам это не подходит.
- Прилипает хэдэр к СВОЕЙ зоне прокрутки: у страницы это scroll-контейнер
  контентной зоны, у скроллящейся карточки — сама карточка.
- Смежное: sticky-вариант Section Header в компонент сознательно не вынесен
  (BACKLOG); Header S умеет sticky ([../plans/dogfood-headers.md](../plans/dogfood-headers.md)).
  Эта догма о поведении в продуктовых экранах, не о правке компонентов.

Живой пример — `demo/app-host` (Header L страниц Events / Management).
